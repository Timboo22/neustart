services:
  db:
    image: postgres:17
    container_name: neustart_db_container
    environment:
      POSTGRES_DB: neustart_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
